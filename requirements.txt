# M8 Kit Creator - Runtime Requirements
# Install with: pip install -r requirements.txt

# Audio Processing
pydub>=0.25.1

# GUI
customtkinter>=5.0.0
tkinterdnd2>=0.3.0

# Bundled ffmpeg (optional but recommended)
# This allows the app to work without system ffmpeg
static-ffmpeg>=2.5
